// References the user input and connects it to the available shapes

class Shapes {
	constructor(textColor, shapeColor, text) {
		this.text = text;
		this.textColor = textColor;
		this.shapeColor = shapeColor;
	}
}
module.exports = Shapes;